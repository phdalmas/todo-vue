<script setup>
    const props = defineProps(['tarefas']);
</script>

<template>
    <div class="container">
        <ul class="list-group mt-4">
            <li class="list-group-item" v-for="tarefa in props.tarefas">
                <input @change="evento => tarefa.finzalida = evento.target.checked" :checked="tarefa.finzalida" :id="tarefa.titulo" type="checkbox">
                <label :class="{done: tarefa.finzalida}" class="ms-3" :for="tarefa.titulo">
                    {{tarefa.titulo}}
                </label>
            </li>
        </ul>
    </div>
</template>

<style scoped>
    .done{
        text-decoration: line-through;
    }
</style>